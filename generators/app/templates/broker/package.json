{
  "name": "<%= app_name %>-broker",
  "version": "1.0.0",
  "description": "<%= app_desc %> service - service broker",
  "main": "server.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@sap/sbf": "^6.0.0"
  },
  "engines": {
    "node": "^12.0.0"
  }
}
